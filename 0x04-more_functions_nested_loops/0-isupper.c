#include "main.h"
/**
 * _isupper - function that checks for uppercase character
 * @x: interger
 * Return: 1 if yes and 0 if no
 */

int _isupper(int x)
{

if (x >= 65 && x <= 99)
{
	return (1);
}
	return(0);
}
