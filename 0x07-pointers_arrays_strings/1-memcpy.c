#include "main.h"
/**
 * _memcpy - copies memories
 * @n: unsigne interger
 * @dest: destination
 * @src: sorce
 * Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{

	int i = 0;

	for (; n > 0; i++)
	{
	s[i] = b;
	n--;
	}
	return (s);

}
