#include <stdio.h>
#include <stdlib.h>
/**
 * main - main entry
 *
 * Return: 0
 */
int main(void)
{


int a;

	for (a = 97; a  <= 122; a++)
	{
		if (a == 101 || a == 113)
		;
		else
		{
			putchar(a);
		}
	}
	putchar(10);
	return (0);

}
